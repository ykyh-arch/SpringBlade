<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <artifactId>SpringBlade</artifactId>
        <groupId>org.springblade</groupId>
        <version>${revision}</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>

    <artifactId>blade-service-api</artifactId>
    <name>${project.artifactId}</name>
    <packaging>pom</packaging>
    <description>SpringBlade 微服务API集合</description>

    <modules>
        <module>blade-desk-api</module>
        <module>blade-dict-api</module>
        <module>blade-system-api</module>
        <module>blade-user-api</module>
        <module>blade-scope-api</module>
        <module>blade-demo-api</module>
    </modules>

    <dependencies>
        <dependency>
            <groupId>org.springblade</groupId>
            <artifactId>blade-starter-mybatis</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-starter-openfeign</artifactId>
        </dependency>
        <dependency>
            <groupId>net.dreamlu</groupId>
            <artifactId>mica-auto</artifactId>
            <scope>provided</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <skip>true</skip><!--默认跳过 Spring Boot 的打包流程，不生成可执行 JAR。通常用于多模块项目中，某些子模块（如纯库模块）不需要独立运行。-->
                    <finalName>${project.name}</finalName><!--指定生成的 JAR 文件名（覆盖默认的 ${project.artifactId}-${project.version}）-->
                </configuration>
            </plugin>
            <plugin>
                <groupId>com.spotify</groupId>
                <artifactId>docker-maven-plugin</artifactId>
                <version>${docker.plugin.version}</version>
                <configuration>
                    <skipDockerBuild>true</skipDockerBuild><!--默认跳过 Docker 镜像构建。通常通过 Maven 命令行参数动态启用（如 -DskipDockerBuild=false）-->
                </configuration>
            </plugin>
        </plugins>
    </build>

</project>
